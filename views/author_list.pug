extends layout

block content
  h1= title
  
  ul
    each author in author_list
      li 
        a(href=author.url) #{author.name} 
        |  (#{author.lifespan_formatted[0]} - #{author.lifespan_formatted[1]})

    else
      li There are no authors.
